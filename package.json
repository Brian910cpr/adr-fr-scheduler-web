{
  "name": "adr-fr-scheduler-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute adr_fr --file=./migrations/001_init.sql",
    "migrate:remote": "wrangler d1 execute adr_fr --remote --file=./migrations/001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251004.0",
    "hono": "^4.9.10",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.0"
  }
}
